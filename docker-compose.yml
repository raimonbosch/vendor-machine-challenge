services:
  vending-machine:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /var/www
    volumes:
      - .:/var/www:cached
    networks:
      - vending-machine-network
    command:
      tail -f /dev/null


networks:
  vending-machine-network:
    driver: bridge